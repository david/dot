- name: template
  ansible.builtin.template:
    src: worktree.desktop.j2
    dest: "{{ home }}/.local/share/applications/{{ box }}-{{ worktree }}.desktop"
    mode: "0644"
  loop: "{{ hostvars[box].worktrees }}"
  loop_control:
    loop_var: worktree
