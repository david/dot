#!/usr/bin/env bash

if [[ -v CONTAINER_ID ]] ; then
  >&2 echo "can't configure host inside a container"
  exit 1
fi

case "$HOSTNAME" in
  aurora)
    sudo systemctl enable --now sshd
    ;;

  timbuktu)
    ;;

  *)
    >&2 echo "unknown host: $HOSTNAME"
    exit 1
    ;;
esac
