#!/bin/bash

set -e

cd /run/host/var/home/david/Boxes/${CONTAINER_ID}

setcap 'cap_net_bind_service=+ep' /usr/bin/traefik

chown david:david .local .local/share

sudo -u david mkdir -p .config .cache trunk .local/bin
sudo -u david /run/host/var/home/david/Boxes/sys/trunk/base/dot-local/bin/restow
