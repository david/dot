#!/bin/bash

choice=$(ls ~/Worktrees | fzf --select-1 --exit-0)

[[ -z $choice ]] && exit

kitty \
  --detach \
  --directory=~/Worktrees/$choice \
  --session=$choice.session.conf \
  --listen-on=unix:/tmp/kitty-$choice

