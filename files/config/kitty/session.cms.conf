os_window_class timbuktu-agent
launch --cwd=current --title=agent direnv exec . gemini

new_os_window
os_window_class timbuktu-services
launch --cwd=current direnv exec . docker compose up db

new_os_window
os_window_class timbuktu-repl
launch --cwd=current direnv exec . docker compose exec dev iex -S mix phx.server

new_os_window
os_window_class timbuktu-shell
launch --cwd=current bash -i

new_os_window
os_window_class timbuktu-git
launch --cwd=current direnv exec . lazygit

new_os_window
os_window_class timbuktu-editor
launch --cwd=current direnv exec . nvim
