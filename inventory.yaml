distrobox:
  children:
    boxes:
      hosts:
        dot:
        chr:
        agt:

  vars:
    ansible_connection: containers.podman.podman
    ansible_python_interpreter: /usr/bin/python3
    box_defaults:
      init: true
    worktrees:
      - trunk
