new_tab
launch --cwd=current --title= lazygit

new_tab
enabled_layouts fat:bias=75

launch --hold --cwd=current --title=󰞷 iex -S mix phx.server
launch --hold --cwd=current --title= postgres -D data/postgres

new_tab
launch --cwd=current --title=

new_tab
launch --cwd=current nvim '+Telescope find_files'
