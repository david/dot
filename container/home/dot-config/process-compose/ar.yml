processes:
  app:
    command: "rails server"
  css:
    command: "yarn run build:css --watch"
  js:
    command: "yarn run build --watch"
  mysql:
    command: "mysqld --datadir=/var/home/david/Projects/ar/.local/share/mysql-data"
  redis:
    command: "redis-server"
